---
"@entwico/bonsai": major
---

rewrite each entrypoint in place next to its original — preloads and worker paths that pointed at files relocated into the first entry's directory must now use the original entry paths
